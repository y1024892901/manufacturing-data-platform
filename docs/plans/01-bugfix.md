# 01. 缺陷修复

> **依赖**：无（可随时插入，建议最优先） · **预估**：半轮 · **对应验收项**：① ② ③ ④ ⑤ 全部——缺陷是五项达标的**前置条件**，其中 4 条 ⭐ 直接阻塞 ① 与 ②
>
> 缺陷清单来源 [../current-status.md](../current-status.md) 的「已确认的缺陷」表（编号 1–23，共 23 条）；分工见 [README.md](README.md)。

## 目标

把 [../current-status.md](../current-status.md) 已确认的 **23 条缺陷逐条修掉**，其中 **4 条（⭐）导致现有功能根本跑不通**——修完这 4 条，已写好的代码才真正可用。本计划**不加新功能**，只把「写了但用不了」变成「能用」，因此投产比高于铺新代码。

## 现状

23 条缺陷中有 4 条属「功能存在但完全不可用」，其余为口径不一致、死代码、校验缺失与工程化问题。按性质分布：

| 性质 | 条数 | 编号 |
|---|---|---|
| ⭐ 现有功能根本跑不通 | 4 | #1 #2 #3 #10 |
| 权限码/鉴权缺口 | 2 | #5 #14 |
| 死代码与未接线 | 4 | #6 #8 #9 #13 |
| 前端契约与一致性 | 5 | #15 #16 #17 #18 #19 |
| 安全与工程化 | 3 | #11 #20 #23 |
| 文档口径不符 | 2 | #21 #22 |
| 说明与实际不符 | 3 | #4 #7 #12 |

**开工前必读：工作区已有并发修复。** 截至本文撰写，工作区存在**未提交**修改，已实测命中 #1、#2、#4、#7、#12 五条（详见下表各行「工作区已修」标注），另有 #13 已实际解决。**开工第一步应先核对 `git status`**，把这些改动确认后提交或回退，避免重复劳动。

## 优先修复（⭐ 现有功能根本跑不通）

四条 ⭐ 中，#1、#2 已有未提交的工作区修复，#3、#10 尚无。这四条的共同点是「代码看起来齐备、调用必失败」，因此也最容易被漏掉：

| # | 现象 | 为什么「根本跑不通」 |
|---|---|---|
| #1 | 能建 BOM 但不能改 BOM | 三处守卫引用**不存在**的权限码，且**没有 `or hasRole('ADMIN')` 兜底**——含 ADMIN 在内任何角色都被拒 |
| #2 | 客户 360 直接报表不存在 | 查询 `md_customer_contact`，该表**全仓无建表语句**；实际表名是 `md_partner_contact` 且列结构不同（`partner_type`/`partner_id`） |
| #3 | 能源主数据分发空转 | `writeToEnergy()` 是 `return 0;`；**进一步核对发现该分支不可达**——`BizType` 的 12 个取值中无一 `targetSystems` 含 `energy` |
| #10 | 数据范围规则全部失效 | 切点 `@annotation(dataScope)` **全仓 0 个使用点**（唯一出现处是 javadoc 示例），`sys_data_scope` 约 30 条规则全部不生效——销售代表能看到全部客户订单 |

## 任务拆解

> 每条一行，逐条对应 [../current-status.md](../current-status.md) 缺陷表的编号。「位置」「问题」保留原表表述，「修复方式」「验证方式」为本次新增。
> **执行归属**：#15–#19 由 [08-frontend-reports.md](08-frontend-reports.md) 执行、#5 的权限码补齐由 [00-foundation.md](00-foundation.md) F1-02 执行——此处保留完整修复与验证方式作为口径基准，避免两处不一致。

| # | 位置 | 问题 | 修复方式 | 验证方式 |
|---|---|---|---|---|
| ⭐1 | `BomController:22,25,26`（`source-apps/mdm/src/main/java/com/mfg/mdm/controller/BomController.java`） | 三个接口守卫 `hasAuthority('MDM:BOM:UPDATE')`，但该权限码**不在权限字典中**（MDM 权限码全集无此项），任何角色含 ADMIN 都无法调用——现象是「能建 BOM 但不能改 BOM」 | **工作区已修**（未提交）：三处已改为 `MDM:BOM:CHANGE`（已定义且已授予 `PRODUCT_ENGINEER`/`PROCESS_ENGINEER`）。二选一并提交：①**接受复用**——零迁移，但需确认「改 BOM」的授权范围就是这两个角色（`PROCESS_SUPERVISOR`/`RND_MANAGER` 未授）；②**正名**——走 V39 迁移补种 `MDM:BOM:UPDATE` 并授权。注意 `MDM:BOM:CHANGE` 的中文名是「BOM变更申请」，复用属语义借用，会把「能改 BOM」与「能提变更」混为一谈 | `PUT /api/mdm/boms/{id}`：以 `PROCESS_ENGINEER` 调用返回 200（此前含 ADMIN 在内必 403）；以无该权限账号调用返回 403。`POST`/`DELETE …/lines/{lineId}/substitutes` 两接口同法各验一次 |
| ⭐2 | `CrmP2Service.customer360()`（`source-apps/crm/src/main/java/com/mfg/crm/service/CrmP2Service.java:19`） | 查询 `src_mdm.md_customer_contact`，该表**全仓无建表语句**；MDM 实际建的是 `md_partner_contact`（V16）——客户 360 的联系人查询会直接报表不存在 | **工作区已修**（未提交）：改查 `src_mdm.md_partner_contact WHERE partner_type='CUSTOMER' AND partner_id=(SELECT id FROM src_mdm.md_customer WHERE customer_code=?)`。需确认 `partner_type` 取值大小写与 `V16` 的 `md_partner_contact` 定义一致，然后提交 | `GET /api/crm/customer-360/{code}` 返回 200 且 `contacts` 为数组（此前必然「表不存在」错误）；对一个确有联系人的客户，`contacts` 条数 > 0。另确认其余 6 段（`opportunities`/`quotations`/`contracts`/`receivables`/`complaints`）表名均真实存在 |
| ⭐3 | `MasterDataDistributor.writeToEnergy()`（`source-apps/mdm/src/main/java/com/mfg/mdm/service/MasterDataDistributor.java`） | 方法体为 `return 0;`，能源的 MDM 分发是**空实现**，`energy_md_equipment` 建表后从未被写入 | 三步：①**先调研**——`BizType`（`source-apps/mdm/…/domain/BizType.java`）的 12 个取值中**没有任何一个** `targetSystems` 含 `energy`，故该 switch 分支**不可达**，仅改方法体无效；②确定「能源需要哪些主数据」——当前 `energy_equipment_usage.equipment_code` 是自由文本、无校验；③在相应 `BizType` 的 `targetSystems` 加 `energy`，并把 `writeToEnergy()` 写成真实的 `INSERT … ON DUPLICATE KEY UPDATE`（照抄 `writeToEam()`）。**前置阻塞**：MDM 的 12 类主数据中**没有「设备」类型**，而 `energy_md_equipment` 需要的正是设备编码——完整修复需先由 MDM 定义设备主数据，**需先调研并决策**（是否新增 `BizType.DEVICE`）。若暂不新增，则最小修复是「明确登记能源不消费 MDM 分发」并关闭本条 | 发布一条主数据后：`SELECT COUNT(*) FROM src_energy.energy_md_equipment` > 0；`src_mdm.md_distribution_log` 出现 `target_system='energy'` 且 `status='SUCCESS'` 的行（当前永不出现）。若采纳「不消费」结论，则以 `md_distribution_log` 无 energy 行 + 文档登记为验收 |
| 4 | `P3CrudService.sc()`（`source-apps/shared/common/src/main/java/com/mfg/common/service/P3CrudService.java`） | SRM 的 `supplier-quality` 状态接口按 `status` 列判定，实际表列名为 `eight_d_status`——该接口任何调用必被拒 | **工作区已修**（未提交）：`sc()` 已加 `case"supplier-quality"->"eight_d_status";`。提交即可。**同类隐患**：`sc()` 是硬编码 switch，`srm_purchase_order` 实际列名为 `order_status` 已单独列出，但 `quotes`/`performance`/`inventories` 返回 `null`（无状态字段）——建议补一条注释说明每个 kind 的状态列来源 | `POST /api/srm/supplier-quality/{id}/status`，body `{"status":"CLOSED"}` 返回 200，且 `SELECT eight_d_status FROM src_srm.srm_supplier_quality WHERE id=?` 随之变化（此前抛「对象没有状态字段」）。回归：`/api/srm/quotes/{id}/status` 仍按「无状态字段」正确报错 |
| 5 | `PlmCatalogController`（`source-apps/plm/src/main/java/com/mfg/plm/controller/PlmCatalogController.java`） | 使用 `PLM:PRODUCT:UPDATE` / `PLM:DOCUMENT:UPDATE` 守卫，两权限码在任何 SQL 中都不存在 | 补齐两码并授予角色——**执行归 [00-foundation.md](00-foundation.md) F1-02**（与该计划其余 4 个未定义码同批迁移，避免抢同一批权限码）。**实际影响小于原表述**：三处 `@PreAuthorize` 均为 `hasAnyAuthority('PLM:PRODUCT:UPDATE','PLM:DOCUMENT:UPDATE','PLM:ECN:CREATE') or hasRole('ADMIN')`，而 `PLM:ECN:CREATE` **已定义且已授予**——故 ADMIN 与持 `PLM:ECN:CREATE` 者可正常调用，受影响的仅是无该码的 PLM 角色 | 以仅持新补 `PLM:PRODUCT:UPDATE` 的非 ADMIN 角色调 `POST /api/plm/catalog/{type}` 返回 200；SQL 复查两码在 `sys_permission` 与 `sys_role_permission` 均有行。**待确认**：若 `PLM:ECN:CREATE` 已覆盖全部 PLM 授权角色，本条可降级为「字典补录」而非功能阻塞 |
| 6 | `EngineeringChangeService.implement()`（`source-apps/plm/src/main/java/com/mfg/plm/service/EngineeringChangeService.java`） | 有实现但 Controller 无 `/implement` 映射，为死代码 | **本次核对确认属实**：`EngineeringChangeController` 只映射 `GET /{id}` 与 `POST /{id}/submit`，却注入 `EngineeringChangeService`；该服务的 `public EngineeringChange implement(Long id)` **全仓无调用者**（全仓唯一的 `.implement(` 调用在 chain controller）。**同时存在第二条路径**：`EngineeringChangeChainController` 的 `POST /api/plm/ecns/{id}/implement` → `EngineeringChangeChainService.implement()`，是另一套实现。**需先决策**：两条路是否各有语义？建议保留 chain 路径、**删除** `EngineeringChangeService.implement()` 以免两套实施逻辑分叉；若语义不同，则分别命名并各自补端点 + 权限码 | 决策后：全仓 `.implement(` 的调用点与端点数量一一对应，不存在「有方法无端点」的 implement；`POST /api/plm/ecns/{id}/implement` 仍可正常实施并生成 MDM 新版本（依赖 #7 先修） |
| 7 | `@GetMapping("/{type:ecrs\|ecos}")`（`EngineeringChangeChainController.java:3`） | 链服务的 `page("ecns")` 分支不可达 | **经复核：这不是用户可见缺陷，无需修复。** `GET /api/plm/ecns` 被两个 Controller 同时映射——`EngineeringChangeController`（类级 `/api/plm/ecns` + 无路径 `@GetMapping`）与 `EngineeringChangeChainController`。Spring 的「字面量优先」规则使前者胜出，**因此只补正则并不会生效**（试改后已回退）。唯一消费者 `ChangeChainView.vue` 的表格是**动态列**（`Object.keys(rows[0])`），拿 JPA 实体照样正确渲染；两个期望实体结构的视图（`EcnView`/`EcnWorkspaceView`）本身是死代码。链服务的 `ecns` 分支属**死代码**，若需清理归 02 计划。 | 无（`GET /api/plm/ecns` 当前行为正确）。若将来要改由链服务提供，须先移除 `EngineeringChangeController` 的列表映射 |
| 8 | `src_eam.eam_equipment_status_log`（DDL 见 `infra/db-init/05_business_systems_2.sql:392`） | 有 DDL 无实体无接口，设备状态切换不落台账 | 新建 `EquipmentStatusLog` 实体 + repo + 查询端点，并在设备状态切换处写台账（前态、后态、起止时间、操作人）。**注意 EAM 目前没有 `service/` 层**，且 `EquipmentRepairController.complete` 的三次写入（维修单 + 关故障 + 改设备状态）**本就不在同一事务**——建议借本条一并补 `@Transactional`，否则台账会与状态不一致 | 切换任一设备状态后 `SELECT COUNT(*) FROM src_eam.eam_equipment_status_log` 增加；台账端点返回该记录且含前后状态与时间。与 [00-foundation.md](00-foundation.md) F4-08 为同一件事，以该计划模板为准执行 |
| 9 | `WorkflowController` / `WorkflowQueryService` | 审批回调放在非标准的 `callback/` 包，而非约定的 `workflow/` | 二选一：①把 `com.mfg.workflow.callback` 迁入约定位置——当前该包下有 `ApprovalCallback` 及 5 个实现类（`CrmApprovalCallback`、`ErpApprovalCallback`、`MdmApprovalCallback`、`EcoApprovalCallback`、`EngineeringChangeApprovalCallback`），需同步更新 `ApprovalEngine` 的 import；②修订约定文档承认 `callback/`。**建议①**——`system-functional-catalog.md` 的代码标准里只有 `workflow/` | 迁移后全仓 `grep com.mfg.workflow.callback` 为 0；跑通一条真实审批（如 MDM 物料创建 → 审批通过）确认 `ApprovalEngine.invokeCallback()` 仍被触发、状态正确推进、分发事件照常发出 |
| ⭐10 | `DataScopeAspect`（`source-apps/shared/security/src/main/java/com/mfg/security/scope/DataScopeAspect.java`） | 切点 `@Around("@annotation(dataScope)")` **全仓库 0 个使用点**，`sys_data_scope` 中已配的约 30 条规则全部不生效——销售代表能看到全部客户订单，与设计意图相反 | **本次核对确认**：`@DataScope(` 全仓唯一出现在 `DataScope.java:13` 的 javadoc 示例里。修复分三步，**建议单独立项**（体量与「铺开鉴权」相当，不宜塞进半轮）：①在 8 类资源（`CUSTOMER`/`OPPORTUNITY`/`SALES_ORDER`/`WORK_ORDER`/`EQUIPMENT`/`INSPECTION`/`PURCHASE_ORDER`/`SUPPLIER`）的查询入口加 `@DataScope`；②把 `DataScopeContext` **真正接到查询 SQL 的过滤条件上**——仅加注解不生效，这是本条的真正工作量；③**先决策两处设计**：`resolveScope()` 只读 `data_scope_type`、**从不解析 `data_scope_value`**（`PUT /api/admin/users/{id}/data-scope` 写入的值是死数据）；且「未配置规则 → 返回 `ALL`」是宽松默认，漏配即等于全开 | 以销售代表账号调客户订单/商机列表，只返回本人（`SELF`）或本部门（`DEPT`）数据；以经理账号调同一接口返回全部；以 ADMIN 调返回全部。**验收前需确认 `data_scope_value` 是否纳入实施范围**，否则「按指定客户列表授权」这类规则仍不生效 |
| 11 | `shared/masterdata` | 空壳模块（`src/` 下仅 2 行 `package-info.java`），10 个模块 pom 声明依赖但全仓 0 行引用；消费方（crm/erp/wms）改为直接依赖 `app-mdm` 并 import `com.mfg.mdm.*`，耦合未消除 | **需先决策**，两条相反的路：①实现三职责（只读副本读取、发布校验、分发客户端），消费方改依赖本模块；②**删除该模块**——移除 10 个 pom 的依赖声明与 `source-apps/pom.xml` 的 `<module>`/`dependencyManagement` 条目，并逐步消除 `com.mfg.mdm.*` 硬依赖。**建议②**：该模块「无唯一价值」，而 10 个模块为了让别人读主数据，把整个第 10 个系统打进了自己的编译依赖 | 若删除：10 个 `pom.xml` 无 `shared-masterdata` 依赖、聚合 pom 同步清理，`mvn -q compile` 全量通过；若实现：crm/erp/wms 不再 `import com.mfg.mdm.*`。本条与 [00-foundation.md](00-foundation.md) 风险 5 为同一事项，勿两处各改一半 |
| 12 | `bootstrap/application.yml:37` | 注释称「各业务系统的库通过多数据源切换（见 shared/common 的 DataSourceConfig）」，但全仓库不存在 `DataSourceConfig` 或任何 `AbstractRoutingDataSource`；真实机制是单数据源 + 限定名 SQL | **工作区已修**（未提交）：注释已改为「单数据源 + 跨库限定名访问：实体用 `@Table(catalog="src_xxx")`…本工程不使用多数据源切换」。**已核对 HEAD line 37 确为旧注释**，提交即可。建议同时检查全仓是否还有同源表述（`MasterDataDistributor` 的类注释已正确说明「无需配置多数据源」） | 全仓 `grep -r DataSourceConfig` 为 0；`application.yml` 注释、`MasterDataDistributor` javadoc、[../../infra/PLAN.md](../../infra/PLAN.md) 三处「单数据源 + 跨库限定名」口径一致 |
| 13 | `SrmP3Controller.java` | 未被 git 跟踪（`??`），却是 `P3FlowService` 的唯一调用方——被清理则该服务完全失去调用方 | **本次核对：已解决**。`git ls-files` 显示 `source-apps/srm/src/main/java/com/mfg/srm/controller/SrmP3Controller.java` **已被跟踪**，`git status` 中不再出现 `??`。无需修复，仅需关闭该条并确认「`P3FlowService` 的唯一调用方」这一脆弱性是否要一并加固（建议在 `P3FlowService` 加注释指明调用方） | `git ls-files \| grep SrmP3Controller` 有输出；`git status --porcelain` 无该文件的 `??` 行 |
| 14 | `mfg_ops.biz_outbox` | P3 链写 `SRM`/`WMS`/`QMS`，P2 模块写 `crm`/`erp`/`mes`/`srm`，系统编码大小写混存 | **本次核对确认**：`publish("QMS.INSPECTION.JUDGED","QMS",…)`、`publish("QMS.NCR.DISPOSED","QMS",…)` 用大写，而 `crm`/`erp` 各发布点用小写，混存于同一表。修复三步：①选定统一口径（**建议小写**，与 P2 现有 13 个发布点及 `SecurityConfig` 之外的既有约定一致）；②在 `BusinessEventService.publish()` 入口做大小写归一化，防止再次混入；③写迁移 `UPDATE` 归一化历史数据 | 归一后 `SELECT DISTINCT source_system, target_system FROM mfg_ops.biz_outbox` 全部小写；新发一条 QMS 事件后再查仍为小写。[00-foundation.md](00-foundation.md) F2-01 的事件路由表必须按此统一口径编写，否则消费方按 `target_system` 匹配会**漏掉一半事件** |
| 15 | `src/views/`（`apps/web-portal/src/views/`） | 5 个文件不被任何路由引用且无构建产物（`DashboardView`、`EcnView`、`EcnWorkspaceView`、`ShellView`、`SystemWorkspaceView`）——死代码 | **执行归 [08-frontend-reports.md](08-frontend-reports.md) 任务 2**。删除前**必须先完成 08 的任务 14 模板迁移**——`DashboardView.vue` 是全工程唯一使用 `.metric-card` 三件套的页面，样式需先迁入新建的 `src/shared/components/MetricCards.vue`，否则会丢样式。**本次核对补充**：五个文件确认为零引用（无 `import.meta.glob`/`require.context` 之类隐式装载机制，`dist/assets/` 也无对应 chunk），但 **`EcnWorkspaceView.vue` 值得「接线」而非删除**——其实现（含创建/提交/实施 ECN 与四步引导）比 `EcnView.vue` 完整，而 `/plm/ecns` 当前指向 `modules/plm/ChangeChainView.vue`（`router/index.ts:75`）；建议二选一：删除，或把它挂到 `/plm/ecns`。另发现一处**文档路径错误**：`source-apps/plm/STATUS.md:122` 称该文件在 `apps/web-portal/src/modules/plm/EcnView.vue`，该路径不存在（实为 `src/views/EcnView.vue`），修本条时一并更正 | `pnpm build`（`vue-tsc -b && vite build`）通过；全仓 grep 五个文件名零引用；若采纳「接线」，`/plm/ecns` 渲染 ECN 实施界面且可完成一次实施 |
| 16 | `systemCatalog.menus` vs `router/index.ts`（`apps/web-portal/src/shared/systemCatalog.ts`、`apps/web-portal/src/router/index.ts`） | 菜单与路由**分开声明且互不校验**，导致 12 条路由没有侧边栏入口；`BusinessWorkspaceView` 声明的 22 个 `kind` 仅 8 个可达 | **执行归 08 任务 4 与任务 5**。①为 12 条路由补菜单（`/mdm/categories`、`/mdm/units`、`/mdm/organizations`、`/mdm/cost-centers`、`/mdm/subjects`、`/qms/sampling`、`/qms/capas`、`/qms/8d`、`/srm/quality`、`/srm/performance`、`/wms/receipts`、`/wms/transfers`）——补进 `systemCatalog.ts` 的 mdm/srm/wms/qms 段；②加**契约测试**断言「每条子路由都有菜单项」且「每条菜单都能命中路由」；③删除 14 个不可达 `kind` 配置（可达的仅 8 个：`mes`、`mesReports`、`eamEquipment`、`eamInspection`、`eamFault`、`eamRepair`、`energy`、`energyWorkshop`），并把 `configs[props.kind] \|\| configs.crm` 的**静默兜底改为显式空态**（`BusinessWorkspaceView.vue:104`，当前未知 kind 会显示 CRM 数据）。**本次核对补充**：错配是**单向**的——全部 69 条目录菜单（10 系统 59 + 门户 8）都能命中真实路由，**没有悬空菜单**，只有 12 个「有页面无入口」；唯一渲染处 `src/layouts/SystemLayout.vue:12` 直接 `v-for` 原始数组、无任何过滤或断言。故契约测试只需锁「路由 ⊆ 菜单」方向 | 契约测试通过；12 条路由可从侧边栏到达；新增路由但漏配菜单时测试**失败**；传入未知 kind 显示空态而非 CRM 行 |
| 17 | `ReferenceAdminView.vue`（`apps/web-portal/src/modules/platform/ReferenceAdminView.vue`） | 读 `route.meta.kind`，但路由只传 `props: { kind }` 且组件未声明该 prop——实际靠 `route.path.split('/').pop()` 兜底，**目前可用是偶然** | **执行归 08 任务 6**。**本次核对补充两点**：①组件（该文件第 20 行）确无 `defineProps`，`route.meta.kind` 恒为 `undefined`，故始终走路径兜底；路由在 `router/index.ts:23-24`，只传 `props` 与 `meta.admin`、**未传 `meta.kind`**，故声明的 `props: { kind }` 是空转。②**还有一处潜在错误**：该行三元 `=== 'roles' ? 'roles' : 'organization'` 会把**任何**非 `'roles'` 值塌缩成 `'organization'`，将来加第三个字典会静默渲染组织表。修法二选一并只保留一种机制：组件声明 `defineProps<{kind?:'roles'\|'organization'}>()` 并用 `props.kind`（去掉塌缩）；或在路由补 `meta.kind`。同目录其余视图（`PartnerView.vue`、`CrmP2View.vue`、`P3BusinessView.vue` 等）均已正确声明 `defineProps`，本文件是唯一例外 | `/portal/roles`、`/portal/organization` 两页仍正确渲染各自字典；改路由路径末段不再影响分支判定；传入第三个 kind 值不再被静默当作 organization |
| 18 | `MaterialView.vue` / `BusinessWorkspaceView.vue`（`apps/web-portal/src/views/`） | 分页不一致：前者写死 `page=1&size=20` 无法翻页；后者表头「共 N 条」显示的是当前页条数而非总数 | **执行归 08 任务 7**。**本次核对定位到行**：`MaterialView.vue`（第 1–2 行）`load()` 写死 `params:{page:1,size:20}`，虽取了 `total` 并显示「共 {{total}} 条记录」，但**没有 `page` ref**，也无任何 `pagination`/`TablePager`/`el-pagination`（grep 零命中），故第 21 条起永远不可见；`BusinessWorkspaceView.vue` 第 177 行渲染 `共 {{ rows.length }} 条`（当前页条数），第 124 行同样写死 `params:{page:1,size:50}` 且**从不读 `totalElements`**。修法：两处都接 `TablePager`，并照 `ReferenceAdminView.vue:17-18,123` 的既有范式（`v-model:page` / `v-model:size` / `:total` / `@change`）；`BusinessWorkspaceView` 另需补 `page`/`size` 状态与「换 kind 时重置页码」的 watch | 物料超过 20 条可翻到第 2 页；工作区页表尾「共 N 条」等于接口返回的 `totalElements`（而非当前页条数） |
| 19 | `api/http.ts`（`apps/web-portal/src/api/http.ts`） | 请求拦截器从 `localStorage` 直读令牌，而非从 Pinia store 读——同一份状态有两个真相来源 | **执行归 08 任务 9**。拦截器（`http.ts:4`）改从 Pinia `auth` store（`apps/web-portal/src/stores/auth.ts`）读令牌。**本次核对补充两点，直接影响修法**：①**存在循环依赖**——`stores/auth.ts` 已 import `../api/http`，故 `api/http.ts` **不得在模块顶层 import store**，必须在拦截器回调内惰性调用 `useAuthStore()`（Pinia 装好之后）；②**真正危害是 401 后的内存态过期**——`http.ts:14-16` 的 401 分支只 `localStorage.removeItem` 而**不清 store**，而路由守卫读的正是 `auth.token`（`router/index.ts:122-123`），故 401 后 SPA 仍自认已登录，直到整页刷新或跨标签页事件；修法应让 401 分支调 `auth.clearLocal()`。注：两侧当前共用 `mfg_token` 键，故取值暂时一致，缺陷表现为「状态源重复 + 401 后不一致」而非立即报错 | 登录、登出、401 跳转三条路径行为不变；**401 后路由守卫立即判定为未登录**（当前需刷新）；外部改写 `localStorage` 不影响已建立的会话；`pnpm build` 无循环依赖告警 |
| 20 | `ops/demo_master_data_lifecycle.py:190,205` | **明文硬编码 MySQL root 口令**，且已进入公开仓库——见 [../current-status.md](../current-status.md) 上方安全警示 | 四步，**第 1 步不可省略**：①**立即轮换该口令**——这是唯一真正有效的措施，改代码不会撤回已公开的内容；②脚本改为从 `infra/.env`（`MYSQL_*` 键）或环境变量读取，不再写死；③检查该 MySQL 实例是否公网可达，若可达风险等级进一步上升；④如需从仓库历史清除，需重写 `6cbe906` 之后的历史并强推——**即使如此已公开内容也无法收回，不能替代第 1 步**。本文档**不复写该口令值** | 轮换后旧口令无法连接；全仓 `grep` 不到该口令字面量；以新口令跑通 `ops/demo_master_data_lifecycle.py`；确认 `infra/.env` 仍被 `.gitignore` 排除 |
| 21 | 库数口径（`infra/db-init/01_databases.sql` 文件头、`mysql8/README`、`db-init/README`） | `01_databases.sql` 文件头写「20 个」、`mysql8/README` 写 20、`db-init/README` 写 18，实测 `CREATE DATABASE` 为 **18** | 统一为实测值 **18**。**本次核对细化到行**：`01_databases.sql` **自身互相矛盾**——第 2 行文件头写「创建全部 20 个数据库」，而同一文件第 33、47 行都写 18；故实际只需改**该文件第 2 行**，外加 `mysql8/README.md` 的**两处**（第 42 行表内「本项目 20 个库」与第 103 行「能看到本项目的 20 个库」）。实测 18 = 10 个 `src_*` + 8 个 `mfg_*`，与该文件自身的一/二/三节标题（10 + 4 + 4）自洽。**只改注释文本，不改 SQL 语句** | 三处口径一致，且等于 `grep -c "CREATE DATABASE" infra/db-init/01_databases.sql`（实测 =18）；`01_databases.sql` 内部不再自相矛盾。**表数口径需一并核对（待确认）**：该文件第 47 行写「96 张表」，而 [../../infra/PLAN.md](../../infra/PLAN.md) 写「99 表」，两者不一致，本次未核实何者为准 |
| 22 | `08_seed_data.sql` | 文件头写「28 个岗位角色 / 29 个演示账号」，实测各为 **36** | 把注释改为实测 **36 / 36**。**本次核对细化到行并确认**：该文件**四处**口径过时——第 4 行「28 个岗位角色」、第 6 行「29 个演示账号」、第 52 行「二、28 个岗位角色」、第 191 行「四、29 个演示账号」；实测第 56 行 `INSERT INTO sys_role` 块（57–105 行）含 **36** 行、第 197 行 `INSERT INTO sys_user` 块（198–249 行）含 **36** 行，与 `09_role_permission.sql` 中出现的 36 个角色码一致。该文件第 576–586 行自带的验证 SELECT 本就会输出 36，属注释陈旧。属 `infra/db-init/` 通道（已 baseline），**只改注释、不改数据**。建议的耐久修法：让这四处数值由验证 SELECT 派生而非手工维护 | 四处注释与实际一致（36/36）；`SELECT COUNT(*) FROM mfg_auth.sys_role` = 36、`mfg_auth.sys_user` = 36。核对命令：两条 INSERT 块的行数统计 |
| 23 | `ops/demo_approval_boundary.py` | 失败时不 `sys.exit(非0)`（结尾只打印 `N/11 通过`），无法被 CI 据此判定失败——同目录的 `demo_bom_approval.py` 反而有 `sys.exit(1)` | **本次核对确认并细化：两个脚本都有问题**。①`demo_approval_boundary.py` 全文件 `grep sys.exit` **零命中**，`len(results)` 恒为 11（恰好 11 个 `check(...)`），故永远是「9/11 通过」但退出码 0；它还**缺 `if __name__ == "__main__":` 守卫**（导入即执行）。修：结尾改 `sys.exit(0 if passed == len(results) else 1)` 并补该守卫。②`demo_bom_approval.py` 的 4 处 `sys.exit(1)`（42/64/88/106 行）**只覆盖 API 硬错误**，其**最终结论**分支（133–136 行：末态非 `APPROVED` 只打印 `✗` 后 `main()` 返回）**仍退出 0**——故「对齐既有写法」不足以修好，需在该 `else` 分支补 `sys.exit(1)` | 人为制造失败：`python ops/demo_approval_boundary.py; echo $?` 非 0；对 `demo_bom_approval.py` 造一个末态非 `APPROVED` 的场景，退出码同样非 0；两者全通过时均为 0。建议与 [00-foundation.md](00-foundation.md) F1-05 的校验脚本一并纳入 CI |

## 完成标准

- [ ] 23 条逐条修完或**明确关闭**（#13 属已解决、#3 若采纳「不消费」结论则需登记结论）——每条在 [../current-status.md](../current-status.md) 的缺陷表中标注状态；
- [ ] 4 条 ⭐ 全部可复现验证通过：#1 `PUT /api/mdm/boms/{id}` 对非 ADMIN 返回 200；#2 `GET /api/crm/customer-360/{code}` 返回 200；#3 能源分发有落库记录或有明确「不消费」结论；#10 销售代表看不到全部客户订单；
- [ ] 工作区已有的并发修复（#1、#2、#4、#7、#12）已确认并**提交**，不与本计划重复劳动；
- [ ] #20 的口令**已轮换**（不是仅改代码），仓库中不再出现该字面量；
- [ ] #16 的契约测试落地——「菜单与路由漏配」由测试而非人工发现；
- [ ] [../current-status.md](../current-status.md) 的「已确认的缺陷」表由 23 条更新为「已修复」或「已登记为后续」，并同步 `docs/README.md` 的文档口径；
- [ ] 本计划**未新增任何业务功能**——所有改动的性质是修正既有代码、口径与配置。

## 风险与前置

1. **并发修改是最大风险**：工作区正在被其他工作并发修改（本文撰写期间已实测到 #1、#2、#4、#7、#12 的未提交修复）。开工前**必须先 `git status` 对账**，否则会出现「同一缺陷两边各修一次」或「覆盖对方改动」。建议本计划开工前先把工作区收敛成一个干净提交。
2. **权限码修法有两条路，需一次定死**：#1 与 #5 都涉及「补种缺失权限码」还是「复用已有码」。若选补种，应与 [00-foundation.md](00-foundation.md) F1-02 的迁移**同批**，避免两个迁移抢同一批权限码；若选复用，需在文档中写明语义借用，否则后续会有人「顺手修正」而改回缺失码。
3. **#3 可能无法在本计划内闭环**：`writeToEnergy()` 的分支不可达，且 MDM 的 12 类主数据中没有「设备」类型，而 `energy_md_equipment` 需要设备编码。完整修复需先新增 `BizType.DEVICE`（属新增主数据类型，超出「不新增功能」的边界）。**建议本条只做调研与决策**，实现移入 [02-mdm-plm.md](02-mdm-plm.md) 或 [07-energy.md](07-energy.md)。
4. **#10 的体量被低估**：补 `@DataScope` 注解只是第一步，真正的工作量在「把 `DataScopeContext` 接到查询 SQL 上」，且 `data_scope_value` 从未被解析。半轮内**不建议**动工；建议只做「登记 + 8 类资源清单」，实现单独立项。
5. **#11 与 #6 都是「先决策后动手」**：删模块 vs 实现、删方法 vs 补端点，两条路的工作量与影响面完全不同。未决策前不要动代码，否则会做出难以回退的中间态。
6. **#15 有删除顺序依赖**：`DashboardView.vue` 承载全工程唯一的 `.metric-card` 样式，必须先完成 [08-frontend-reports.md](08-frontend-reports.md) 任务 14 的 `MetricCards.vue` 迁移再删，否则报表基座会缺样式。
7. **#21 / #22 是「只改注释」**：两者都位于 `infra/db-init/` 通道，而 [README.md](README.md) 已约定「不再改动 `infra/db-init/`」。此处是**改注释文本、不改任何 SQL 语句与数据**，不违反该约定；但改动时需明确只碰注释行。
8. **`partner_type` 取值待确认**：修 #2 时用 `partner_type='CUSTOMER'`，需与 `V16` 建表注释及 `MdmGovernanceService` 的既有写入值核对大小写，否则联系人仍查不到（会从「报表不存在」变成「静默返回空数组」，更难发现）。
9. **#14 的归一化会影响消费方匹配**：[00-foundation.md](00-foundation.md) 的消费框架按 `target_system` 路由，故本条必须在消费方落地**之前**完成归一，否则历史事件会漏消费。建议 A/B 两条一起做。
