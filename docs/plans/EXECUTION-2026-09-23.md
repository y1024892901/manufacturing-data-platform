# 2026-09-23 计划执行记录

## 范围与起点

实际仓库为 `D:/manufacturing-data-platform`，计划目录为 `docs/plans`。
开始时已有大量未提交的 F1、F2、F4 改动；保留并续接，没有整体回退、提交或推送这些改动。
本轮聚焦计划 00 的运行缺口与计划 01 的关联缺陷。00/01 尚未全部完成，02–08 未开始本轮实施。

## 本轮交付

| 计划项 | 结果 | 验证 |
|---|---|---|
| F1 端点基线 | 在已有注解/权限迁移基础上完成核对；新增 6 个端点均有鉴权 | 266 端点，263 有注解、3 白名单，静态权限缺口 0；角色 HTTP 验收待补 |
| F1 异常响应 | 方法级 AccessDenied 不再被通用处理器转为 500，返回 HTTP 403 / 20002 | MockMvc 回归测试 |
| F2-02 迁移 | 修复 V40 部分 DDL 执行后不能重跑的问题；小写归一化使用 BINARY 比较 | 官方 Flyway repair + migrate 成功，版本至 40.2 |
| F2-03/04 | 显式事务、租约所有权、按事件与目标匹配唯一消费者、JSON 失败重试、业务效果与消费位点原子提交 | 6 项调度器单元测试；额外 1 项嵌套事务测试证明统一回滚 |
| F2-05 | 死信 GET 与重放 POST 接口；非法状态为业务错误、最多 200 ID、整批回滚 | 重放拒绝与去重测试通过 |
| F2-07 | 选择 WMS 收货→QMS 检验单一条真实消费试点，消除该路径直写 | 3 项消费者测试；真实 DB CONSUMED/重复事件/重复扫描烟测通过，测试数据回滚 |
| F3-04/06 | 四类 MDM 停用 API、悲观写锁、前后状态审计、DISABLED 事件与同批授权 | 2 项测试覆盖四类停用、重复拒绝、审批中拒绝；HTTP 全生命周期待补 |
| F4 | 补角色时间、库存交易字段；BOM 替代料显式列名消除静态误报 | 已有实体列缺口 0；123 张无实体表仍待登记，不标记整体完成 |
| 01 #14 | 发布入口强制 source/target 小写，历史数据迁移归一化 | 真实 DB 烟测通过 |
| 01 #23 | 审批边界脚本增加 main 守卫；既有失败非零退出保留 | 语法及无网络导入检查；两条完整审批 E2E 未重跑 |

## 迁移记录

- 开始时数据库 V39 成功、V40 失败，但 V40 的字段/索引/位点表/视图均已存在。
- 将未应用的 V41 端点权限迁移改为 V40.1，避免侵占计划 02 编号；新增停用权限 V40.2。
- 用户明确要求「不需要备份，执行到40.2」；按该范围执行官方 Flyway repair 与目标限定为 40.2 的 migrate，共成功应用 3 条。
- V40、V40.1、V40.2 已应用后不可再修改其 SQL；后续修正开新迁移，且需遵守后续获准执行范围。
- 打包时清除 target/classes 中旧 V41 生成资源，避免旧文件被重新打入 JAR。

## 验证命令

使用 Java 17（默认 PATH 为 Java 8，直接 Maven 测试会遇到 class version 61/52 不匹配）。

```powershell
$env:JAVA_HOME='D:/jdk-17.0.20'
$env:PATH="$env:JAVA_HOME/bin;$env:PATH"
mvn -o -f source-apps/pom.xml package
python -X utf8 ops/check_permissions.py --strict
python -X utf8 ops/check_entity_schema.py
python ops/run_foundation_smoke.py --java D:/jdk-17.0.20/bin/java.exe
```

22 项 Java 测试通过（0 failures / 0 errors）。真实数据库烟测通过；包含 Spring 默认 REQUIRED 嵌套事务不会独立提交的独立回归测试。
实体脚本严格模式仍会因 123 张无实体表失败，不能把普通模式零列差异当作完整 F4 验收。

## 下一步

1. 完成 F4 裸 SQL 表逐表归属登记、实时 information_schema 校验、MES/EAM/SRM 实际写入点验收。
2. 完成 F1 业务角色/越权角色 HTTP 403 验收，以及 F3 四类创建→审批→发布→停用全生命周期。
3. 继续 01 的数据范围、空模块处置等缺陷；口令是否已轮换不能从源码修正推定，仍需单独核实。
4. 按依赖顺序推进 02–07，再完成 08 的前端与报表，不预先勾选任何未通过的完成标准。
